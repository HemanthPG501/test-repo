{
  "name": "nokia-config-manager-deployment",
  "version": "1.0.0",
  "description": "Deploy partial config ZIP to Nokia Configuration Manager using Playwright",
  "main": "deploy-config-manager.js",
  "scripts": {
    "deploy": "node deploy-config-manager.js"
  },
  "dependencies": {
    "@playwright/test": "^1.45.0"
  }
}